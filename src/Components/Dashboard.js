import { Button } from '@material-ui/core';
import React from 'react';

class Dashboard extends React.Component {
    render() {
      return (
        <Button variant="contained" color="primary">Dashboard Button</Button>
      );
    }
}

export default Dashboard;
